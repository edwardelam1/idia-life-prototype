
## Sync from GitHub (`main` + `feature/android-apple-parity`)

Both branches converge at commit `42ad0af` ("Android parity for Apple review changes"). After diffing GitHub against the current Lovable project, the meaningful new work is a Capacitor/Android share-sheet path in `nativeDownload.ts` plus the Android Capacitor project scaffold. Two governance edge functions on GitHub are broken (unresolved merge markers / truncated file) and must NOT overwrite the current Lovable versions.

### Adopt from GitHub

1. **`src/utils/nativeDownload.ts`** — replace with GitHub version.
   - Adds Android branch using `@capacitor/core`, `@capacitor/filesystem`, and `@capacitor/share`: writes payload to app cache then invokes the Android share sheet (parity with iOS `UIActivityViewController`).
   - Keeps existing iOS WKWebView `nativeDownload` bridge and web `<a download>` fallback untouched — no change to the iOS path already verified working.
   - Adds a `toBase64()` helper for Blob/string payload conversion.
   - Requires runtime deps `@capacitor/core`, `@capacitor/filesystem`, `@capacitor/share` (Capacitor is already used by the project per `capacitor.config.ts`; confirm the two plugins are installed and add them if missing).

2. **`android/` directory** — add the Capacitor Android project (build.gradle, gradle wrapper, `app/`, keystore, proguard rules, etc.). Needed so Shawn can build the Android shell that consumes the new Capacitor-based download flow. No changes to any existing project files.

3. **`contracts/Contracts/MAINNET_DEPLOYMENT.md`** — add (documentation only, no code impact).

### Do NOT overwrite (GitHub is stale/broken)

- **`supabase/functions/governance-indexer/index.ts`** — GitHub version is missing the entire `serve(...)` request handler body. Current Lovable file is the correct env-first-governor version. Keep as-is.
- **`supabase/functions/relay-governance-action/index.ts`** — GitHub version still contains unresolved `<<<<<<< Updated upstream / >>>>>>> Stashed changes` conflict markers around the ABI block. Current Lovable file resolves these and adds V3 quorum errors + env-first governor. Keep as-is.

### Skip (auto-managed or environment)

- `src/integrations/supabase/types.ts` — generated by Supabase, never hand-edit.
- `.env`, `package-lock.json`, `supabase/.temp/*` — local/CI artifacts.

### Feature-executability impact

- **iOS Identity-Ledger export / native share flow** — unchanged code path (`window.webkit.messageHandlers.nativeDownload`), so the working iOS build stays working.
- **Android Identity-Ledger export / any native download** — starts working via the new Capacitor branch once the two plugins are installed and the Android shell is rebuilt.
- **Governance (indexer, relay, quorum reads, motion escalation, veto, submit-proposal gating, etc.)** — untouched; the current Lovable edge functions and UI logic are preserved.
- **ToS / AoR consent flow, Synapse Credits row, wallet nudge, health auto-sync, focus modes, etc.** — untouched (already present in both trees).

### Post-adoption verification

- Confirm `@capacitor/filesystem` and `@capacitor/share` appear in `package.json`; install if missing.
- Trigger `nativeDownload(...)` on iOS Safari-in-shell → still routes through the WebKit bridge (log line `iOS: raw WKWebView bridge`).
- Trigger the same call in the Android Capacitor shell → new log line for filesystem write + share-sheet invocation.
